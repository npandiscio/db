---
import Container from '../shared/Container.astro';
import Icon from '../shared/Icon.astro';

const currentPath = Astro.url.pathname;
const base = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`;
---

<header class="fixed top-0 w-full z-50 bg-white/94 dark:bg-background-dark/94 backdrop-blur-md border-b border-[#d9d9d9] dark:border-white/10 shadow-sm">
  <Container class="h-20 flex items-center justify-between">
    <!-- Logo -->
    <a href={base} class="flex items-center gap-3 group cursor-pointer">
      <div class="text-primary">
        <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 48 48">
          <path d="M24 0.757355L47.2426 24L24 47.2426L0.757355 24L24 0.757355ZM21 35.7574V12.2426L9.24264 24L21 35.7574Z" />
        </svg>
      </div>
      <h1 class="text-xl font-black tracking-tighter uppercase text-brand-ink dark:text-white">Architects</h1>
    </a>

    <!-- Desktop Navigation -->
    <nav class="hidden md:flex items-center gap-10">
      <a 
        href={base} 
        class:list={[
          "text-sm font-semibold uppercase tracking-[0.2em] transition-colors",
          currentPath === '/' 
            ? "text-primary" 
            : "text-brand-ink/75 dark:text-slate-300 hover:text-primary dark:hover:text-primary"
        ]}
      >
        Home
      </a>
      <a 
        href={`${base}projects`} 
        class:list={[
          "text-sm font-semibold uppercase tracking-[0.2em] transition-colors",
          currentPath === '/projects' || currentPath.startsWith('/projects/')
            ? "text-primary" 
            : "text-brand-ink/75 dark:text-slate-300 hover:text-primary dark:hover:text-primary"
        ]}
      >
        Projects
      </a>
      <a 
        href={`${base}about`} 
        class:list={[
          "text-sm font-semibold uppercase tracking-[0.2em] transition-colors",
          currentPath === '/about'
            ? "text-primary" 
            : "text-brand-ink/75 dark:text-slate-300 hover:text-primary dark:hover:text-primary"
        ]}
      >
        About
      </a>
    </nav>

    <!-- CTA -->
    <div class="flex items-center gap-4">
      <a 
        href={`${base}contact`} 
        class="bg-primary hover:bg-[#0095cd] text-white px-6 py-2.5 rounded text-sm font-bold uppercase tracking-[0.18em] transition-all flex items-center gap-2 shadow-sm"
      >
        <Icon name="send" size={16} />
        <span>Inquire</span>
      </a>
      
      <!-- Mobile menu button -->
      <button class="md:hidden p-2 text-brand-ink dark:text-slate-300" aria-label="Open menu">
        <Icon name="menu" size={24} />
      </button>
    </div>
  </Container>
</header>
